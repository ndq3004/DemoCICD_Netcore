pipeline {

    agent any

    stages {
        stage("build"){

            steps {
                echo "build"
            }
        }

        stage("test"){

            steps {
                echo "test12"
            }
        }

        stage("deploy"){

            steps {
                echo "deploy"
            }
        }
    }
}
